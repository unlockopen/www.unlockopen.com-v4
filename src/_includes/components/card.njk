<li class="card">
  <div class="flow">
    {% if headingContext === "h2" %}
      <h2>
        <a href="{{ item.url | url }}">{{ item.data.title }}</a>
      </h2>
    {% else %}
      <h3>
        <a href="{{ item.url | url }}">{{ item.data.title }}</a>
      </h3>
    {% endif %}
    <div class="cluster">
      <span class="text-step-min-1">
        {% set definedDate = item.date %}
        {% include "components/date.njk" %}
      </span>
    </div>
    {% if item.data.lede %}
      <p>{{ item.data.lede | markdown | safe }}</p>
    {% endif %}
  </div>
</li>
